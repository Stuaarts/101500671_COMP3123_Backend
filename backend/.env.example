PORT=5000
# Use your hosted MongoDB (e.g., Atlas) for production
MONGO_URI=mongodb+srv://username:password@cluster.example.mongodb.net/comp3123_assignment
# Use a strong, unique value in production
JWT_SECRET=replace-with-secure-secret
# Frontend origin (Vercel/Render/Netlify), used for CORS
CLIENT_URL=https://your-frontend-domain.com
